@model IEnumerable<Maquillaje.WebUI.Models.DepartamentosViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-lg-12 mb-0 order-0">
    <div class="card" style="background-color: white">
        <div class="d-flex align-items-end row">
            <div class="col-sm-12">
         

                <div class="card-body">
                    <a class="btn btn-outline-primary" data-toggle="modal" data-target="#CreateDep">Nuevo</a>
                    <br />
                    <br />


                    <table class="table table-report -mt-2" id="DataTable">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.dep_ID)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.dep_Descripcion)
                                </th>

                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.dep_ID)
                                    </td>

                                    <td>
                                        @Html.DisplayFor(modelItem => item.dep_Descripcion)
                                    </td>

                                    <td>
                                        <a class="btn btn-outline-info" data-toggle="modal" data-target="#EditDepo" onclick="Editar('@item.dep_ID,@item.dep_Descripcion')">Editar</a> |
                                        <a class="btn btn-outline-success" asp-action="Details" asp-route-id="@item.dep_ID">Detalles</a> |
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </div>
</div>

@Html.Partial("_Create");
@Html.Partial("_Edit");
