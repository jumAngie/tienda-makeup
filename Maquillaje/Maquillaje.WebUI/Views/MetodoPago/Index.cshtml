@model IEnumerable<Maquillaje.WebUI.Models.MetodoPagoViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.met_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.met_Descripcion)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.met_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.met_Descripcion)
            </td>

            <td>
                <a asp-action="Edit" asp-route-id="@item.met_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.met_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.met_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
