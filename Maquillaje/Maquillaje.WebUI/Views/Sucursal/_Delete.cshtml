
<form action="@Url.Action("Delete","Sucursal")" method="post" id="deleteform">

    <div class="modal fade" id="DeleteSuc" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-sm modal-dialog-centered">
            <div class="modal-content bg-danger text-white">
                <div class="modal-body text-center">
                    <h3 class="text-white mb-15"><i class="fa fa-exclamation-triangle"></i> Alert</h3>
                    <input type="hidden" id="suc_Id" name="suc_Id" />
                    <div class="text-gray-600 mt-2">
                        ¿Está seguro de eliminar el registro?
                        <br />
                        <br />
                        <button type="button" id="confirmar" class="btn btn-light">OK</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>
<script>

    function ShowModal(suc_Id) {
        $('#DeleteSuc').modal('show');
        $("#confirmar").click(function () {
            var id = suc_Id;

            $.ajax({
                url: "/Sucursal/Eliminar",
                type: "POST",
                data: { suc_Id: id },
                success: function () {
                    window.location.href = "/Sucursal/Listado";
                },
                error: function () {
                    alert("Error al eliminar el registro.");
                }
            });
        });
    };
</script>
